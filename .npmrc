# Exact versions only — see docs/WORKING_DISCIPLINE.md §2.5 (dependency version lock policy)
save-exact=true
# Strict peer deps: surface incompatibilities loudly instead of silently resolving them
strict-peer-dependencies=true
# Reproducible installs
resolution-mode=highest
