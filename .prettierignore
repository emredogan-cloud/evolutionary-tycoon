dist
coverage
node_modules
.vercel
playwright-report
test-results
pnpm-lock.yaml
CHANGELOG.md

# Generated, byte-for-byte, by `pnpm assets:prompts:html`.
# A test asserts the committed file matches a fresh render, so reformatting it breaks the build.
docs/ASSET_GENERATION_PROMPTS.html

# Generated by `pnpm balance`. The report is a diff of measured numbers, and
# reformatting it would put Prettier's opinion in every balance diff.
docs/BALANCE_REPORT.md
docs/balance
